# 待办

## 请求取消（独立于 MLX 后端）

- [ ] 单独设计并实现请求取消能力，不纳入本轮 MLX 接入。

当前行为：服务在检查连接或发送 SSE 时可以发现客户端断开，但正在进行的 prefill /
forward 不会因此中断；长请求可能继续占用计算资源和 Session slot，直到计算返回。

后续需要明确：

- HTTP / 请求层产生取消信号，Session 负责请求生命周期；后端只处理计算边界，不认识
  HTTP 连接。统一讨论 CPU、CUDA、Metal、MLX 的支持范围，不增加 MLX 专用取消语义。
- 选择可停止的计算边界，区分停止后续调度与中断正在执行的 GPU kernel，并测量取消
  到释放 slot 的延迟。
- 明确部分更新的 cache、checkpoint 和 logits 如何处理，确保后续请求不会复用无效状态；
  请求信号不能因 reset 丢失，也不能在请求结束后留下悬空引用。
- 独立验证长 prefill、decode、工具输出缓冲期间的断开，以及取消后的资源释放和请求恢复；
  明确 SSE 与非流式请求的行为。

旧的 MLX 专用实现与验收记录保留在 `backup/mlx-before-review-20260908` 和
`build/qwen36-night/`，仅供设计参考，不代表本轮实现支持计算中途取消。

## MLX 运行时错误恢复（有证据后再设计）

- [ ] 出现真实错误后，先确定其可恢复性，再考虑请求级恢复能力。

当前策略：请求边界已校验的输入错误正常返回；模型加载失败沿已有初始化路径报错并退出；
forward 捕获执行异常，记录操作和原因后终止进程。State 创建、重置和 checkpoint 拷贝
直接执行，不逐个捕获异常；未捕获异常由 C++ 运行时终止，不保证统一的项目日志。
不添加 runtime 失败查询、失败后自动重置 Session 或继续接收请求的机制。

未来若要恢复，需要针对具体错误验证：失败计算的资源和执行队列可继续使用，部分更新的
cache/checkpoint 可以安全丢弃或重建，且后续请求的结果仍与干净状态一致。捕获异常、
重置自有 State 或 mock 故障测试通过，都不能单独证明真实 MLX 错误可恢复。
