{
  "name": "q3x",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run main.ts",
    "check": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build ./main.ts --compile --minify --no-compile-autoload-dotenv --no-compile-autoload-bunfig --outfile build/q3x"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
