[project]
name = "qwen35-eval"
version = "0.1.0"
description = "Reproducible EvalScope benchmarks for qwen35-0.8b"
requires-python = ">=3.10"
dependencies = [
    "evalscope[ifeval]==1.10.0",
]

[tool.uv]
package = false
