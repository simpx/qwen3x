name: Course checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  course:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run every lesson
        working-directory: from-scratch
        run: make test
      - name: Build the real-model program and run its self-test
        working-directory: .
        run: make test
