[project]
name = "qwen3x-reference"
version = "0.1.0"
description = "Official Transformers oracle for qwen3x"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115,<1",
    "numpy>=1.26",
    "torch>=2.6",
    "transformers>=5.0.0",
    "uvicorn>=0.34,<1",
]

[tool.uv]
package = false
